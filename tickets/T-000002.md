---
id: T-000002
title: Implement QwenLLMService extending Pipecat BaseLLMService
status: ready
priority: p0
type: code
effort: m
labels:
- backend
tags:
- pipecat-qwen
owner: null
created: '2026-04-27T10:56:57Z'
updated: '2026-04-27T10:56:57Z'
depends_on:
- T-000001
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Write `server/qwen_llm_service.py` — a Pipecat-compatible LLM processor that routes transcript text through Qwen via OpenRouter and emits frames the pipeline can consume.

## Acceptance Criteria
- [ ] File `server/qwen_llm_service.py` exists and is importable
- [ ] Class extends the correct Pipecat 1.0.0 LLM base (from T-000001 audit)
- [ ] Emits `LLMFullResponseStartFrame` → `TextFrame(text=...)` → `LLMFullResponseEndFrame` per turn
- [ ] Reads `OPENROUTER_API_KEY`, `OPENROUTER_BASE_URL`, `OPENROUTER_MODEL` from env; falls back to `QWEN_API_KEY` if set
- [ ] All control/system frames pass through unchanged (no dropped `CancelFrame`, `EndFrame`, etc.)
- [ ] Smoke test passes: `python -c "from server.qwen_llm_service import QwenLLMService; print('ok')"`

## Notes
- OpenRouter env vars already exist in `.env` from the current rizerve-stack setup
- Non-streaming (single-shot) response only — streaming is T-000003
- Use `httpx.AsyncClient` (already a dependency) for the OpenRouter call
- System prompt: "You are a helpful real-time voice assistant. Be concise."
- Difficulty: HIGH — frame protocol must match exactly or pipeline silently drops output

## Agent Assignment
- Suggested owner: agent-llm
- Suggested branch: feature/qwen-llm-service

## Implementation Plan
- [ ] Read T-000001 spike output to get exact base class import and required method signatures
- [ ] Implement `process_frame`: on `LLMMessagesFrame`, call OpenRouter; push start/text/end frames
- [ ] Pass all other frames through with `await self.push_frame(frame, direction)`
- [ ] Add `__main__` block for manual smoke test
- [ ] Run `python server/qwen_llm_service.py` and confirm no import errors

## Queue Lifecycle (if allocated)
- [ ] Add progress with `mt comment T-000002 "..."`
- [ ] If blocked/failing, run `mt fail-task T-000002 --error "..."`
- [ ] On completion, move to `needs_review` then `done`
