---
id: T-000007
title: Env var config integration example and README update
status: ready
priority: p2
type: docs
effort: xs
labels:
- docs
tags:
- pipecat-qwen
owner: null
created: '2026-04-27T10:57:07Z'
updated: '2026-04-27T10:57:07Z'
depends_on:
- T-000005
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Document the full run instructions, add all required env vars to `.env.example`, and provide a minimal `server/voice_pipeline_example.py` showing the pipeline in 50 lines with clear integration comments.

## Acceptance Criteria
- [ ] `.env.example` contains all new keys: `QWEN_API_KEY` (optional), `TTS_VOICE`, `PIPECAT_PORT`
- [ ] `README.md` has a "Voice Assistant (Pipecat + Qwen)" section with: prerequisites, `pip install -r requirements.txt`, and `npm run start:voice`
- [ ] `server/voice_pipeline_example.py` is a self-contained 50-line example with comments at each pipeline stage explaining why each service is there
- [ ] Comments explain the three Pipecat integration points: frame protocol, `allow_interruptions`, and how to swap Qwen for another LLM
- [ ] `requirements.txt` updated if any new pip packages were needed (e.g., `pipecat-ai[cartesia]`)

## Notes
- Keep the example short — it should be the "copy this to get started" reference, not a full production file
- Integration points to comment: `DeepgramSTTService` → why Deepgram (streaming, low latency), `QwenLLMService` → how to swap, `DeepgramTTSService` → Cartesia upgrade note
- Link to Pipecat 1.0.0 migration guide in README if one exists

## Agent Assignment
- Suggested owner: agent-docs
- Suggested branch: docs/voice-pipeline-readme

## Implementation Plan
- [ ] Audit `.env.example` against all services used — add any missing keys
- [ ] Write README section (under existing content, do not replace)
- [ ] Write `server/voice_pipeline_example.py` as annotated minimal version of T-000005 output
- [ ] Run `markdownlint README.md` if available, otherwise manual review
- [ ] Validate `requirements.txt` with `pip install -r requirements.txt --dry-run`

## Queue Lifecycle (if allocated)
- [ ] Add progress with `mt comment T-000007 "..."`
- [ ] If blocked/failing, run `mt fail-task T-000007 --error "..."`
- [ ] On completion, move to `needs_review` then `done`
